# ============================
# SERVER CONFIG
# ============================
server.port=8080


# ============================
# MYSQL DATABASE CONFIG
# ============================
# spring.datasource.url=jdbc:mysql://localhost:3306/troubleshooters?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=Mohit@005

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}


spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# ============================
# JPA / HIBERNATE CONFIG
# ============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


## ============================
# SPRING MAIL CONFIG
# (We will use later for sending accept/selection emails)
# For now keep dummy details. You can add Gmail or SMTP later.
# ============================

spring.mail.host=smtp.gmail.com
spring.mail.port=587

# spring.mail.username=mail2moht@gmail.com
# spring.mail.password=ylnytmuvxzvtpnys

spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}


spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
